<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sale Order Form View Inherit -->
    <record id="view_sale_order_form_commission" model="ir.ui.view">
        <field name="name">sale.order.form.commission</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_commission_lines" type="object" 
                        class="oe_stat_button" icon="fa-money"
                        invisible="commission_count == 0">
                    <field name="commission_count" widget="statinfo" string="Commissions"/>
                </button>
            </xpath>
            <xpath expr="//page[@name='other_information']" position="after">
                <page string="Commission" name="commission_info">
                    <group>
                        <group>
                            <field name="commission_generated"/>
                            <field name="total_commission_amount"/>
                        </group>
                        <group>
                            <button name="action_generate_commission" string="Generate Commission" 
                                    type="object" class="oe_highlight"
                                    invisible="commission_generated == True"/>
                        </group>
                    </group>
                    <field name="commission_line_ids" readonly="1">
                        <tree>
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="commission_amount"/>
                            <field name="state"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Sale Order Tree View Inherit -->
    <record id="view_sale_order_tree_commission" model="ir.ui.view">
        <field name="name">sale.order.tree.commission</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total']" position="after">
                <field name="total_commission_amount" optional="hide"/>
                <field name="commission_generated" optional="hide"/>
            </xpath>
        </field>
    </record>

</odoo>