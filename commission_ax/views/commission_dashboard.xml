<?xml version="1.0"?>
<odoo>
    <data>
        <record id="view_commission_kanban" model="ir.ui.view">
            <field name="name">commission.kanban</field>
            <field name="model">sale.order</field>
            <field name="arch" type="xml">
                <kanban default_group_by="commission_status">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="amount_total"/>
                    <field name="total_commission_amount"/>
                    <field name="commission_status"/>
                    
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_global_click">
                                <div class="o_kanban_record_title">
                                    <field name="name"/>
                                </div>
                                <div>Customer: <field name="partner_id"/></div>
                                <div>Total: <field name="amount_total"/></div>
                                <div>Commissions: <field name="total_commission_amount"/></div>
                                <div>Status: <field name="commission_status"/></div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>
        
        <record id="action_commission_dashboard" model="ir.actions.act_window">
            <field name="name">Commission Dashboard</field>
            <field name="res_model">sale.order</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="view_id" ref="view_commission_kanban"/>
            <field name="context">{'search_default_commission': 1}</field>
            <field name="domain">[('is_commission_po', '=', True)]</field>
        </record>
        
        <menuitem id="menu_commission_dashboard" 
            name="Commission Dashboard" 
            action="action_commission_dashboard"
            parent="sale.menu_sale_root"
            sequence="10"/>
    </data>
</odoo>