<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Ultra-minimal Payment Configuration Settings -->
        <record id="res_config_settings_view_form_payment" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.payment</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">

                <!-- Most basic approach - add to form end -->
                <xpath expr="//form" position="inside">
                    <group string="Payment Workflow Settings" invisible="0">
                        <field name="enable_payment_approval_workflow"/>
                        <field name="enable_payment_qr_verification"/>
                        <field name="auto_post_approved_payments"/>
                        <field name="send_approval_notifications"/>
                        <field name="require_voucher_remarks"/>
                        <field name="enable_qr_codes"/>
                        <field name="approval_threshold_1"/>
                        <field name="approval_threshold_2"/>
                        <field name="approval_threshold_3"/>
                        <field name="use_osus_branding"/>
                        <button name="action_view_payment_dashboard" string="Open Payment Dashboard" type="object" class="btn btn-primary"/>
                        <button name="action_test_qr_generation" string="Test QR Generation" type="object" class="btn btn-secondary"/>
                    </group>
                </xpath>

            </field>
        </record>

    </data>
</odoo>