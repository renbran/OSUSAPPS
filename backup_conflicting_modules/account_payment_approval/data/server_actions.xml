<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Server action for bulk submit for approval -->
        <record id="action_bulk_submit_for_approval" model="ir.actions.server">
            <field name="name">Bulk Submit for Approval</field>
            <field name="model_id" ref="account.model_account_payment"/>
            <field name="binding_model_id" ref="account.model_account_payment"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
# Bulk submit multiple payments for approval
if records:
    action = records.bulk_submit_for_approval()
    if action:
        action = action
            </field>
            <field name="groups_id" eval="[(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>
